# ===============================================================
#  CVT 저장소 제외 목록  (2026-08-17)
#
#  2026-08-17 에 저장소 뿌리를 twin\ 에서 CVT\ 로 올렸다.
#  코드·문서·기억은 twin/ 아래에서 계속 관리하고,
#  아래 큰 폴더들은 git 밖에 둔다.
#
#  왜 밖에 두나 (twin/docs/context/BACKUP.md 참조):
#    - 합쳐서 56 GB 다. git 에 넣으면 저장 공간이 두 배가 된다.
#    - 작업 사본(worktree) 만들기가 8초에서 수십 분으로 늘어난다.
#    - 끝난 결과물에 필요한 것은 이력이 아니라 사본이다.
#  안전장치: D:\CVT_backup_260816\  (전체 사본)
# ===============================================================

# 실험 원본 데이터 (3.9 GB) — 읽기 전용
/Data/

# 옛 결과·그림·애니메이션 (34 GB)
/jump_opt/

# 다리 최적화·시스템 동정 작업 폴더 (18.5 GB)
/AVT LEG/

# 순기구학·역기구학 참고 자료 (0.09 GB)
/FKnIK/

# 백업 실행 기록
/_backup_to_D.log

# OS
Thumbs.db
desktop.ini
.DS_Store
