# AGENTS.md (twin 폴더용) — 정본은 한 단계 위에 있다

> ## ▶ **`../AGENTS.md` 를 읽어라.** (= `C:\Users\junho\CVT\AGENTS.md`)
>
> 2026-08-17 에 git 저장소 뿌리가 `twin\` 에서 `CVT\` 로 올라갔다.
> 그래서 **모든 에이전트 공용 안내서는 이제 한 단계 위에 있다.**
> 연구 목적 · 로봇 제원 · 폴더 구조 · 절대 규칙 · **보고 형식** 이 전부 거기 있다.
>
> 이 파일은 `twin` 폴더에서 바로 시작한 도구가 길을 잃지 않도록 남겨 둔 이정표다.
> **여기에 규칙을 새로 적지 마라.** 두 곳에 적으면 서로 어긋나고, 실제로 그 사고가 났었다
> (2026-08-17: 정본 보호 규칙이 세 문서에 서로 다르게 적혀 있었다).

## 최소한 이것만은

| 항목 | 내용 |
|---|---|
| 프로젝트 뿌리 | `C:\Users\junho\CVT` (git 저장소 뿌리이기도 하다) |
| 이 폴더 | 코드·하네스·문서·기억. git 이 실제로 관리하는 것은 사실상 여기 |
| 공용 안내서 | `../AGENTS.md` ← **먼저 읽어라** |
| 지금 상황 | `docs/context/HANDOFF.md` |
| 누적된 발견 133건 | `docs/context/memory/MEMORY.md` |
| 코드 구조 | `CLAUDE.md` (같은 폴더) |
| 현행 모델 수치 단일 출처 | `code/bench/CURRENT_STACK.md` |

## 절대 어기면 안 되는 것 세 가지만 (나머지는 `../AGENTS.md` §3)

1. **`Data\` 아래 실측 원본(xlsx·txt·영상) 내용을 고치지 않는다.** 그 폴더에 분석 코드를
   새로 만드는 것은 된다. 원본 자체를 건드려야 하면 먼저 묻는다.
2. **발밑 힘센서(GRF) 값의 크기를 쓰지 않는다.** 세션마다 교정이 달라 세션 간 비교도 금지.
3. **긴 계산을 직접 실행하지 않는다.** `.bat` 을 만들어 사용자가 더블클릭하게 한다
   (`.bat` 안에는 ASCII/영문만).

## 보고 방식

한국어. **결론 한 줄 → 전후 표 → 판정표 → 발견 → 결정 대기 → 이월 → 랩업.**
저장소 안에서만 통하는 이름과 코드 이름으로 설명을 채우면 실패다.
자세한 것은 **`../AGENTS.md` §4 를 반드시 읽어라.**
