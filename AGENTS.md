# AGENTS.md — 이 저장소에서 일하는 모든 에이전트를 위한 지시 (Codex / Claude / 그 외)

## 가장 먼저 할 일

**`docs/context/HANDOFF.md` 를 끝까지 읽는다.** 연구 배경, 경로 지도, 지금 돌아가고 있는 작업,
누적된 발견, 보고 형식이 전부 거기에 있다. 이 파일은 그중 "어기면 즉시 사고가 나는 것"만 추린 요약이다.

## 이 저장소가 무엇인가

2자유도(엉덩이·무릎) 4절 링크 무단변속기(CVT) 단족 점프 로봇의 **MuJoCo 시뮬레이션 모델을
실제 로봇과 일치시키는 연구**의 코드·하네스·결과물이 들어 있다.
최종 목표는 이 모델로 궤적 최적화를 한 뒤 실제 로봇을 PD 제어했을 때
**측정 토크가 계획 토크와 같아지는 것**이다.

## 절대 규칙 (위반 시 연구 데이터가 손상되거나 몇 주치 결과가 무효가 된다)

1. **실험 원본을 수정하지 않는다.**
   `C:\Users\junho\CVT\Data\` 아래의 xlsx/txt 는 읽기 전용이다.
   교정이 필요하면 읽는 코드 쪽에서만 한다.
2. **발밑 힘센서(GRF) 값의 크기를 쓰지 않는다.** 세션마다 로드셀 교정이 달라 세션 간 비교도 금지.
   허용되는 것은 "닿았나 안 닿았나"와 상대 타이밍뿐이며, 고정 문턱 판정도 금지다
   (각 시행이 자기 창의 낮은 무리와 높은 무리 중간값을 쓴다).
3. **26.03.24 세션은 검증 전용이다. 모델 적합에 절대 넣지 않는다.**
4. **긴 파라미터 훑기를 직접 실행하지 않는다.** `.bat` 파일을 만들어 사용자가 더블클릭하게 한다.
   `.bat` 안에는 **ASCII/영문만** 쓴다 (명령창이 cp949라 한글이 깨진다).
5. `code/goal18_CANONICAL/` 과 `fourbar_*_candidate.json` 은 **수정·덮어쓰기 금지**.
6. **정본 코드를 고친다. 사본을 만들지 않는다.**
7. python 실행 시 환경변수 `PYTHONIOENCODING=utf-8` 을 설정한다 (안 하면 인코딩 오류로 죽는다).
8. **채점에서 구간을 나누지 않는다** (통짜 채점과 나란히 두는 것은 허용).
9. **축 토크 15 N·m 는 사용자의 설계 목표다. 하드웨어 한계가 아니다. 맞는지 다시 묻지 않는다.**

## 지금 돌아가고 있는 작업 — 건드리지 말 것

2026-08-16 18:12 부터 약 20시간, `code/goal23_fullspan/` 에서 파라미터 훑기가 병렬로 돌고 있다
(프로세스 이름은 `python.exe` 가 아니라 **`python3.11.exe`**, 12개 이상).
**그 폴더의 파일을 옮기거나 지우지 않는다.** 상태 확인은 `tasklist | findstr python3.11`.

## 지식이 어디에 있는가

| 찾는 것 | 파일 |
|---|---|
| 지금 상황 전체 | `docs/context/HANDOFF.md` |
| 누적된 발견 41건 (색인 → 개별 문서) | `docs/context/memory/MEMORY.md` |
| 현행 모델 수치 (**단일 출처**) | `code/bench/CURRENT_STACK.md` |
| 이미 기각된 가설 (새 축 열기 전 필독) | `code/bench/REJECTED.md` |
| 방법론 | `code/bench/PLAYBOOK.md` |
| 실험 데이터의 함정 목록 (데이터 만지기 전 필독) | `docs/context/DATA_DICT.md` |
| 연구 헌법 | `docs/context/CONSTITUTION.md` |
| 코드 구조·import 규약·후보 JSON 규약 | `CLAUDE.md` |

## 사용자에게 보고하는 방식

한국어로 쓴다. 순서는 **결론 한 줄 → 적용/미적용 before-after 표 → 판정표 → 발견 →
결정 대기 → 이월 → 랩업 상태**.

단어는 (가) 고등학생도 아는 일상어이거나 (나) 물리·로봇 대학원생이 아는 표준 용어여야 한다.
**이 저장소 안에서만 통하는 이름**(`ModeA`, `a_hat`, `J_G`, `p24`, `FS_PRESLIDE`, `l_i` 등)과
**코드 이름**(환경변수·함수·파일명)으로 설명을 채우면 실패다. 하는 일로 풀어 쓰고,
원래 이름은 꼭 필요할 때 괄호에 한 번만 넣는다.

직접 만든 지표를 쓸 때는 **매번** 네 가지를 같이 쓴다:
① 무엇과 무엇을 비교했는가 ② 어떻게 계산했는가 ③ 완벽하면 얼마인가 ④ 물리적으로 무슨 뜻인가.
