# -*- coding: utf-8 -*-
"""P19 노션 진행 로그 append — 3~5장 (바닥 실험, 게인 발견, 커맨드 층)."""
import json
from pathlib import Path
import sys

HERE = Path(__file__).parent
sys.path.insert(0, str(HERE))
import build_notion_p19 as N

ROOT = json.load(open(HERE / "p19_notion.json"))["root"]

B = [
    N.h1("3. 지표의 바닥 실험 (00:40) — sim 없이 데이터만으로"),
    N.para(N.rt("질문: 실측 (q,dq)를 PD 식에 그대로 넣어 재구성한 토크는 실측 토크와 얼마나 다른가? "
                "이것이 이 지표로 도달 가능한 바닥이다 (sim이 아무리 완벽해도 이보다 못 내려감).")),
    N.table([
        ["재구성 구성", "FIT 갭", "발견"],
        ["F0: 라벨 게인, 무클립", "84.4%", "sim(69%)보다 나쁨 — 라벨 PD가 실측 토크를 설명 못함!"],
        ["F0 + LPF 25Hz", "79.9%", "노이즈 문제가 아니라 저주파 계통 불일치"],
        ["F1: + raw 클립 ±35.5", "76.9%", "포화는 무릎 채널에 실재 (0602 knee 61→32%)"],
        ["F2: 회귀(실효) 게인 + 클립", "27.3%", "★ 펌웨어 실효 게인 ≠ 라벨이 전 세션 공통"],
    ]),
    N.callout("🔑", N.rt("τ-갭의 주범 = 커맨드 층 (라벨 게인 → 실효 거동의 변환): ", bold=True),
              N.rt("kp 압축이 게인이 클수록 심함 (60→×0.9, 150→×0.5~0.7, 200→×0.55) + kd_eff는 라벨의 15~40% "
                   "— 정적 매핑이 아니라 토크 추종 대역폭(1차 지연)의 시그니처. 지연 스캔 결과 tm=8ms 최적 "
                   "(GOAL7의 8.37ms 재발견 — Mode A에선 죽은 축이었던 이유: replay는 이미 실현된 토크라 지연이 안 보임. CL에서만 살아있음).")),
    N.h1("4. 커맨드 층 확정 (01:00)"),
    N.bullet(N.rt("전역(물리): PD 명령 → 1차 지연 tm=8ms → 클립 ±35.5(raw) → a_hat(Paper) → 축토크")),
    N.bullet(N.rt("세션(캘리브레이션): 실효 게인 스케일 α (0421의 0.6× 발견을 전 세션으로 일반화, 데이터만으로 적합): "
                  "0424 [0.72,0.72,0.78,0.72] / 0602 [0.84,0.83,0.84,0.97] / 0421 [0.80,0.78,0.90,0.98] / "
                  "0429 [1.18,0.37,1.58,0.93] (0429는 실제 인가 게인이 폴더 라벨과 다름 — R²>0.95 증거)")),
    N.bullet(N.rt("커맨드 층 적용 후 재구성 바닥: 84.4% → ≈28% (0424 25.2 / 0602 19.5 / 0421 11.7 / 0429 47.5 — 0429는 포화 지배)")),
    N.bullet(N.rt("기각: 속도 필터 tv (다수 세션 악화), 정적 게인 매핑 (지연이 물리적으로 우월)")),
    N.h1("5. sim 재베이스라인 + CMA-2 (01:10)"),
    N.table([
        ["sim 구성 (P18b+paper)", "FIT 갭(v3)", "비고"],
        ["커맨드층 없음", "45.4%", "지표 v3 기준"],
        ["+ 지연/클립", "49.4%", "폐루프에선 궤적 자체가 바뀌어 단조 개선 아님 (0429 악화)"],
        ["+ 커맨드층(α)", "46.0%", "0424/0602 개선, 0429/0421 악화 — 물리 재적합 필요"],
    ]),
    N.para(N.rt("→ CMA-2 발사: 커맨드층 고정 + 물리 15개(스프링·프리로드·마찰·접촉·armature·질량관성·CoM) + tm, "
                "전체 31 trials, 확장 바운드, 700 evals. 바닥(~28%)에 얼마나 접근하는지가 트윈 물리의 성적표.")),
]
N.append(ROOT, B)
print("appended ch3-5")
