# -*- coding: utf-8 -*-
"""P22 마라톤 노션 페이지 (PLAYBOOK §10 표준: 요약 콜아웃→쉬운 설명→수식→그림→표→한계→산출물)."""
# --- 옛 결과 폴더 위치: 단일 출처 (code/bench/datapaths.py) ---
import os as _o3, sys as _s3
_d3 = _o3.path.dirname(_o3.path.abspath(__file__))
while _d3 != _o3.path.dirname(_d3) and not _o3.path.isdir(_o3.path.join(_d3, 'code', 'bench')):
    _d3 = _o3.path.dirname(_d3)
if _o3.path.join(_d3, 'code', 'bench') not in _s3.path:
    _s3.path.append(_o3.path.join(_d3, 'code', 'bench'))
from datapaths import LEGACY_ROOT  # noqa: E402
# ---------------------------------------------------------------
import sys
from pathlib import Path

HERE = Path(__file__).parent
sys.path.insert(0, str(HERE.parent.parent / "bench"))
import notion_kit as N

FIG = Path((LEGACY_ROOT + "/g22_p22_results"))


def loc(txt):
    return N.callout("📁", N.rt("산출물 위치: ", bold=True), N.rt(txt, code=True))


def main():
    root = N.create_page(N.GOAL22, "P22 마라톤 — P19 초월: 편법 없이, 원인 규명으로 (진행중)", icon="🚀")
    N.append(root, [
        N.callout("🎯", N.rt("목적: ", bold=True),
                  N.rt("P21까지의 결론은 '현 구조에서는 폐루프 τ 충실도와 개루프 재생 충실도가 맞바꿈 관계"
                       "(파레토 벽)'였다. P22는 벽 자체를 옮긴다 — 보정층 같은 편법이 아니라, 판별 실험으로 "
                       "원인의 주소지를 확정하고 그 주소만 고친다.")),
        N.callout("📌", N.rt("현재까지 한 줄 요약: ", bold=True),
                  N.rt("시동 전 4건 병렬 조사(문헌 2 + 데이터 판별 3종)로 지형이 뒤집혔다: ① 푸시 부족분은 "
                       "측정이 아니라 트윈 플랜트의 과잉 소산 (에너지 원장 증명) ② '0421만 보정 불필요'라는 "
                       "6일짜리 수수께끼는 데이터 로더 버그(토크 1.35배 인플레)였다 ③ 경계에 붙던 관성 "
                       "나사들은 '데이터가 원리적으로 못 보는 방향(널스페이스)'일 가능성 — 수학으로 판정 중.")),
        N.callout("🧭", N.rt("방법론: ", bold=True),
                  N.rt("골든 재현(REPRODUCED 38.1/35.7) → 지표 v5 선고정(재생 dq·점프높이 포함) → 오염 "
                       "소스 수술 후 재베이스라인 → 베이스 파라미터 분석(식별 가능 조합만 적합) → "
                       "MuJoCo 공식 sysid + 다목적 진화(NSGA-II)로 파레토 전선 전체를 그려 'P19를 양쪽에서 "
                       "이기는 점'의 존재를 직접 판정. 도구는 전부 검증된 오픈소스/공식 배포판.")),
        N.h2("child 페이지"),
        N.para(N.rt("P22-1 쉬운 이야기(에너지 원장과 로더 버그) / P22-2 판별 실험 4건 상세 / "
                    "P22-3 마라톤 헌장(지표 v5·재베이스라인·로드맵)")),
        loc("코드 repo/code/goal22/p22_beyond/ · 판별 스크립트 p20_rise/p22_probe_t1~t3.py · "
            "그림 Desktop/jump_opt/g22_p22_results/ · 기각 대장 repo/code/bench/REJECTED.md"),
    ])

    # ── P22-1 쉬운 이야기 ──
    p1 = N.create_page(root, "P22-1 쉬운 이야기 — 가계부 검사와 1.35배 유령", icon="📖")
    N.append(p1, [
        N.callout("💡", N.rt("3줄 요약: ", bold=True),
                  N.rt("'전류가 말하는 일'과 '실제 점프가 요구한 에너지'로 가계부를 맞춰보니 수지가 맞았다 — "
                       "즉 측정은 정직했고, 느린 것은 우리 시뮬레이션이 에너지를 실물보다 많이 잃기 때문이다. "
                       "그리고 6일간 모든 가설을 죽여온 '0421 예외'는 로더가 엉뚱한 파일을 읽은 버그였다.")),
        N.h2("① 가계부 검사 (에너지 원장) — 시뮬레이션 없이 하는 심판"),
        N.para(N.rt("로봇에 토크 센서가 없어서 우리는 모터 전류로 토크를 추정한다(â). 그동안의 미스터리: "
                    "재생 시뮬에 â를 그대로 넣으면 실물보다 느리게 뛴다 — 그래서 '전류가 토크를 숨긴다"
                    "(과소보고)'는 해석과 '시뮬이 에너지를 흘린다'는 해석이 팽팽했다. 이 둘을 가르는 방법이 "
                    "가계부다: 물리 법칙상, 다리가 한 일(∫â·dq)은 로봇을 그 높이까지 올리는 데 필요한 "
                    "에너지(M·g·Δh, 질량 3.2kg 실측)보다 작을 수 없다. 만약 작다면 â가 거짓말(과소보고)을 "
                    "하는 것이고, 크거나 같다면 â는 충분히 정직한 것이다.")),
        N.para(N.rt("결과: 25개 trial 중 22개에서 '전류가 말하는 일'이 요구 에너지보다 크거나 같았다. "
                    "0424/0602는 다리 자체의 운동에너지까지 계산에 넣으면 수지가 거의 정확히 맞는다. "
                    "판결: 측정은 무죄. 범인은 트윈 플랜트(과잉 감쇠/마찰/오배분)다. 이는 P20c 재적합이 "
                    "감쇠를 깎았을 때 재생이 좋아졌던 관찰과도 맞물린다 — 문제는 그때 폐루프 점수를 "
                    "지키면서 깎는 좌표를 몰랐다는 것.")),
        N.img(str(FIG / "t3_ledger.png"), "trial별 가계부: 파란(전류가 말하는 일) vs 주황(실측 높이가 요구한 에너지). 0429만 파랑이 크게 남는다."),
        N.h2("② 예외 하나가 수상하면 파이프라인을 의심하라 — 1.35배 유령"),
        N.para(N.rt("그동안 '0421 세션만 보정이 필요 없다'는 관측이 여러 가설을 죽이는 앵커였다. 그런데 "
                    "판별 실험 중 우연히 발견: 0421의 재생용 토크를 읽는 로더가 원본 xlsx가 아니라 옛날 "
                    "비교용 csv(predicted_compare.csv)를 읽고 있었고, 그 파일의 토크가 원본의 정확히 "
                    "1.32~1.36배였다(양 채널, 6/6 trial). 즉 0421 재생에는 남들보다 35% 부풀린 토크가 "
                    "들어가고 있었으니 '보정이 필요 없어 보였던' 것이다. 원본으로 통일하면 0421도 다른 "
                    "세션과 같은 급의 보정을 원한다 — 예외가 사라지고, 전 세션이 하나의 법칙으로 통일될 "
                    "가능성이 열렸다.")),
        N.img(str(FIG / "fix0421_overlay.png"), "결정적 그림: csv 곡선(주황 점선)이 원본×1.35(초록 점선)에 정확히 겹친다."),
        N.h2("③ 그래서 P22가 하는 일"),
        N.para(N.rt("오염 소스를 수술(fix0421)하고 자를 다시 맞춘 뒤(재베이스라인), '시뮬이 에너지를 어디서 "
                    "얼마나 잃는지'를 식별 가능한 좌표에서만 다시 적합한다. 자세한 계획은 P22-3.")),
        N.callout("⚠️", N.rt("한계/미해결: ", bold=True),
                  N.rt("0429의 에너지 잉여(+40%)는 별도 이상 — 포화 근방 전류 대역의 â 과대 추정 vs 실제 "
                       "전달 손실을 분해하는 실험이 진행 중. csv를 만든 옛 코드의 정체(1.35배의 기원)는 "
                       "미상 — 사용자도 모름, 원본 xlsx 통일이 안전한 선택.")),
        loc("판별 스크립트 repo/code/goal22/p20_rise/p22_probe_t3.py · 수술 모듈 p22_beyond/p22_fix0421.py"),
    ])

    # ── P22-2 판별 실험 상세 ──
    p2 = N.create_page(root, "P22-2 판별 실험 4건 — 수식과 수치", icon="🔬")
    N.append(p2, [
        N.callout("💡", N.rt("3줄 요약: ", bold=True),
                  N.rt("T1: 0429 게이트는 검정력이 있었다(모터측 기각 재확인). T2: 열/배터리 가설 미지지. "
                       "T3: 에너지 원장이 '측정 과소보고'를 기각하고 범인을 플랜트로 지목. "
                       "문헌: a_hat의 출처·검증 범위가 우리 믿음과 달랐다.")),
        N.h2("T3 — 에너지 원장 (핵심 판별)"),
        N.code_block("W_in = ∫[0,t_lo] (â₁·dq₁ + â₂·dq₂) dt   (전류가 말하는 푸시 일)\n"
                     "E_req = M·g·(h_apex − bz₀),  M = 3.2 kg (실측)   (실측 높이의 요구 에너지 — 하한)\n"
                     "ρ = W_in / E_req   →   ρ < 1 이면 '측정 과소보고' 증명, ρ ≥ 1 이면 측정 무죄"),
        N.table([
            ["세션", "n", "ρ 평균", "해석"],
            ["0421", "6", "0.97", "수지 정합 (측정 무죄)"],
            ["0424", "9", "1.02", "다리 KE 포함 시 거의 정확"],
            ["0602", "6", "1.06", "위와 동일"],
            ["0429 (CVT)", "10", "1.40 (최대 1.71)", "큰 잉여 — 별도 이상 (0c에서 해부 중)"],
        ]),
        N.para(N.rt("민감도: 이륙 시점 ±30ms, 운동학 이륙 재정의 모두 판정 불변. 인코더 FK 탄도 예측과 "
                    "카메라 실측 높이가 평균 +9.4cm 내 정합 — 원장의 신뢰 근거.")),
        N.h2("T1 — 0429 교차게이트 검정력 재심"),
        N.para(N.rt("의심: 'CVT는 토크를 증폭하니 0429의 모터 전류가 낮아서, 전류 제곱형 오차는 0429에서 "
                    "안 보였을 수 있다(기각이 성급했다)'. 검증 결과 반대 — 0429 푸시 전류(중앙값 33.2)가 "
                    "무변속(20.4~28.5)보다 오히려 높았다(크랭크가 무릎보다 빨리 돌아 PD 명령이 클립의 "
                    "94%까지 상승). 따라서 모터측 오차는 0429에서 1.35~2.64배 증폭돼 보였어야 하고, "
                    "안 보였으므로 기각은 옳았다. 단, 부산물 발견: 0429만 |전류|>30 포화 대역에서 "
                    "운전한다 — 그 대역 전용 â 오차는 무변속 세션이 검증할 수 없는 사각지대(0c의 표적).")),
        N.h2("T2 — 세션 내 시간 드리프트 (열/배터리 지문)"),
        N.para(N.rt("어느 세션도 trial 순서에 따른 유의한 보정량 변화 없음 (진짜 취득 순서가 복원되는 "
                    "세션은 0602 하나뿐이라 검정력 약함은 정직하게 명기). 문헌도 방향이 반대(자석이 "
                    "뜨거우면 같은 전류에서 토크가 줄어 â가 과대가 됨) — 열 축은 사망.")),
        N.h2("문헌 — a_hat의 정체"),
        N.bullet(N.rt("실출처는 논문이 아니라 UMich 드라이버 코드(TMotorCANControl)이며, 논문(M-BLUE)의 "
                      "적합 계수와 불일치 — 서로 다른 계수 세트가 공존하고 CF=0.59는 저자 스스로 "
                      "'UNTESTED' 표기.")),
        N.bullet(N.rt("검증 범위 = 정상상태·2Hz 필터·출력 5.2 rad/s 이하. 우리 점프(8~14 rad/s)는 전부 "
                      "검증 밖 외삽 — 이 영역에선 우리 잔차가 세계 최선의 데이터.")),
        N.bullet(N.rt("펌웨어가 보고하는 전류는 측정치(명령 에코 아님, 소스코드로 확인). 유지 구간 부하 "
                      "지지의 물리 후보 = 기어 마찰 콘 ±(0.37+8.8%·τ) — 관측(20~25%)의 약 절반을 설명, "
                      "잔여 절반은 미규명(벤치 표적).")),
        N.callout("⚠️", N.rt("한계: ", bold=True),
                  N.rt("T2는 검정력 부족(시간 기록 부재)으로 '기각 방향'이지 확살이 아님. T3의 E_req는 "
                       "하한이라 잉여의 원인 분해(실손실 vs 추정오차)는 원장만으로 불가 — 0c가 담당.")),
        loc("스크립트 repo/code/goal22/p20_rise/p22_probe_t1.py, t2, t3 · 결과 JSON 같은 폴더 · "
            "문헌 보고서는 세션 로그"),
    ])

    # ── P22-3 마라톤 헌장 ──
    p3 = N.create_page(root, "P22-3 마라톤 헌장 — 지표 v5, 재베이스라인, 로드맵", icon="📜")
    N.append(p3, [
        N.callout("💡", N.rt("3줄 요약: ", bold=True),
                  N.rt("점수를 먼저 정의하고 불변으로 고정했다(v5 = 기존 7성분 + 통짜 재생 dq + 점프높이). "
                       "오염 수술 후 P19의 기준값을 다시 쟀다(창 점수만 이동). 로드맵은 게이트식 5단계.")),
        N.h2("지표 v5 (선고정 — 마라톤 동안 불변)"),
        N.code_block("J_v5 = 0.30·CLτ̂ + 0.15·CLdq̂ + 0.10·JŴ02 + 0.10·JŴ06 + 0.075·Ŝ2S + 0.025·Ô6\n"
                     "       + 0.15·ÔLdq + 0.10·Ĥ      (성분̂ = 성분 / P19_rebased)\n"
                     "OLdq = Mode A 통짜 재생 dq2 RMSE (0424/0602/0429 세션 평균)\n"
                     "Ĥ    = Mode A 재생 |h_sim/h_real − 1| 점프 trial 평균\n"
                     "승격 게이트: CLτ̂·CLdq̂·ÔLdq·Ĥ ≤ 1.00 그리고 JŴ02·JŴ06·Ŝ2S·Ô6 ≤ 1.05\n"
                     "           + held-out(0324) 무악화 + bench REPRODUCED + Mode A 가드"),
        N.h2("재베이스라인 (fix0421 후 P19 기준값)"),
        N.table([
            ["성분", "fix 전", "P19_rebased", "의미"],
            ["CLτ / CLdq", "0.381 / 0.167", "불변", "CL 경로는 원래 원본 직독 — 일관성 체크 통과"],
            ["JW02 (0.2s 창)", "418", "470 (+12%)", "부풀린 토크가 창 점수를 가려주고 있었음"],
            ["JW06 (0.6s 창)", "1236", "902 (−27%)", "과잉 에너지 드리프트 소멸 — 공짜 개선"],
            ["s2s / 0604 / 0429창", "—", "불변", "0421 무관 성분 (수술 범위 검증)"],
        ]),
        N.h2("바닥 (도달 가능선)"),
        N.bullet(N.rt("CLτ 바닥 ≈ 28% (PD 궤적오차 주입분 — P19 마라톤 산출). 헤드룸 38.1→28.")),
        N.bullet(N.rt("재생 dq 바닥 = 측정 노이즈 0.14~0.17 rad/s — 현 오차(1.26~3.31)의 8~20배 아래. "
                      "이 축의 헤드룸은 사실상 전부 모델 개선분.")),
        N.h2("로드맵 (게이트식)"),
        N.table([
            ["Phase", "내용", "상태"],
            ["0", "골든 재현 + fix0421 + 재베이스라인 + 지표 v5", "✅ 완료"],
            ["0c", "0429 에너지 잉여 해부 (전류 대역별 분해)", "진행 중"],
            ["1", "베이스 파라미터 분석 — 식별 가능 관성 조합 산출, 널스페이스 동결", "진행 중"],
            ["2", "mujoco.sysid 재적합 + NSGA-II 파레토 매핑 (양쪽 지배점 탐색)", "대기"],
            ["3", "(벽 잔존 시) PySR 기호회귀로 잔차 식 발굴 — 폐루프 재검증 의무", "대기"],
            ["4", "벤치(사용자): 링크 무게·CoM + 유지전류 매트릭스 + 공중 가진 30분 + 온도/전압", "대기"],
        ]),
        N.callout("⚠️", N.rt("정직한 한계: ", bold=True),
                  N.rt("CL τ-갭엔 PD가 만드는 바닥(28%)이 있어 0%는 불가능. 질량분포 나사는 실측 없이는 "
                       "어떤 방법론으로도 결판 불가 — Phase 1은 '무엇을 실측해야 하는가'를 수학으로 "
                       "특정하는 단계다.")),
        loc("헌장 원본 repo/code/goal22/p22_beyond/MARATHON_p22.md · 재베이스 p22_rebase.json · "
            "평가기 p22_eval.py (구축 중)"),
    ])

    print("root:", root)
    for pid, name in [(root, "root"), (p1, "P22-1"), (p2, "P22-2"), (p3, "P22-3")]:
        ok = N.verify_images(pid)
        print(name, pid, "images:", ok)


if __name__ == "__main__":
    main()
