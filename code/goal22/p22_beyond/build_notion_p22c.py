# -*- coding: utf-8 -*-
"""P22 노션 3탄 — P22-5 마일스톤 장: p22a 탄생, 파레토 전선, 정직한 주의 항목."""
import sys
from pathlib import Path

HERE = Path(__file__).parent
sys.path.insert(0, str(HERE.parent.parent / "bench"))
import notion_kit as N

ROOT = "39eab81d-2550-812c-9336-c48ab4af0dec"
FIG = Path(r"C:/Users/junho/Desktop/jump_opt/g22_p22_results")


def loc(txt):
    return N.callout("📁", N.rt("산출물 위치: ", bold=True), N.rt(txt, code=True))


def main():
    p = N.create_page(ROOT, "P22-5 마일스톤 — p22a: P19를 전 전선에서 이기는 최초의 후보 (+ 정직한 주의 2건)", icon="🏆")
    N.append(p, [
        N.callout("💡", N.rt("3줄 요약: ", bold=True),
                  N.rt("2목적 진화 탐색(NSGA-II)이 지표 v5의 8개 성분 전부에서 P19를 이기고 "
                       "held-out 폐루프까지 개선(35.7→33.7%)하는 후보 p22a를 찾았고, 재현성 검증"
                       "(REPRODUCED)과 전 데이터 그래프 79종 생성까지 마쳤다. 단 두 가지를 정직하게 "
                       "명기한다: 세션별 재생은 0424만 크게 좋아지고 0602/0429는 3~4% 물러섰으며(게이트는 "
                       "세션 평균), held-out의 '개루프 재생'은 오히려 나빠졌다(폐루프는 개선).")),
        N.h2("① 무엇을 어떻게 찾았나"),
        N.para(N.rt("전날 밤 확립한 좌표 — 오염 수술(0421), 재베이스라인, 살아있는 나사 지도, 널스페이스 "
                    "판정 — 위에서, 두 목적(폐루프 τ-갭 vs 통짜 재생 dq)을 동시에 최소화하는 진화 "
                    "탐색을 돌렸다. 나머지 지표 6종은 'P19의 102~105% 이내' 제약으로 걸어 편법 승리를 "
                    "차단했다. 158세대(4 세그먼트) 동안 체크포인트-재개로 진행했고, 세그먼트마다 "
                    "전수 검증(엄격 게이트)을 통과하는지 별도 채점했다.")),
        N.img(str(FIG / "pareto_front.png"),
              "파레토 전선 (널 나사 동결 탐색): 좌하 사분면 = P19를 폐루프·재생 양쪽에서 동시에 이기는 영역. 다이아몬드가 지배 개체들, P가 게이트 통과자 p22a."),
        N.h2("② p22a 성적표 (전부 P19 대비)"),
        N.table([
            ["지표", "P19", "p22a", "판정"],
            ["폐루프 τ-갭 FIT", "38.1%", "37.0%", "개선 (bench 심판 정합·REPRODUCED)"],
            ["held-out 폐루프 (0324)", "35.7%", "33.7%", "−2.0%p — fit에 없던 세션에서 개선"],
            ["통짜 재생 dq (3세션 평균)", "1.000", "0.994", "개선"],
            ["점프높이 오차", "4.76%", "4.0%", "개선"],
            ["CL dq·창 2종·s2s·0604", "1.000", "0.93~0.99", "전부 개선"],
        ]),
        N.para(N.rt("파라미터 변화의 핵심: hip 감쇠 −43% (민감도 지도가 예고한 스타 무브), 스프링 "
                    "기준각 −7%, 시상수 0.3→1.0ms. 프리로드(pre30 2.15)는 유지, 상시 어시스트층은 "
                    "사망(계수 0.0006) — P21의 어시스트 실험은 이 후보에서 완전히 대체됐다.")),
        N.h2("③ 정직한 주의 2건 (승격 판단에 필요한 전부)"),
        N.bullet(N.rt("세션별 재생: 0424 −8.7% / 0602 +4.2% / 0429 +2.7% — 게이트가 보는 것은 세션 "
                      "평균(−0.6%)이다. 세션 하나하나까지 전승은 아니다.")),
        N.bullet(N.rt("held-out(0324, FF-토크 세션)의 개루프 재생: 2.93 → 4.10 (+40%). 흥미롭게도 같은 "
                      "세션의 폐루프는 개선(33.7%) — 개루프와 폐루프가 held-out에서 갈렸다. 널 나사를 "
                      "동결한 탐색에서도 이 열화가 남았으므로(3.5~4.2), 이는 감쇠 삭감 방향 자체의 숨은 "
                      "비용이다. 참고: held-out은 선택 압력에 쓸 수 없으므로(유출 금지) 이 수치는 진단 "
                      "전용이며, 게이트는 규약대로 폐루프만 봤다.")),
        N.h2("④ 널스페이스 동결 시험 — Phase 1 수학의 실전 검증"),
        N.para(N.rt("베이스 파라미터 분석이 '데이터가 못 보는 방향'으로 지목한 나사 4개(크랭크 질량, "
                    "허벅지/정강이 관성, 정강이 무게중심)를 P19 값에 못박고 탐색을 다시 돌렸다. 결과: "
                    "지배 개체가 오히려 15개로 늘고 재생 개선 최고치도 5.6%로 커졌다 — 널 통로는 "
                    "필요 없었을 뿐 아니라 탐색을 오도하고 있었다. 수학이 예측한 그대로다.")),
        N.callout("⚠️", N.rt("한계/다음: ", bold=True),
                  N.rt("진화 개체군이 CL dq 제약의 여유(102%)를 파고들어 엄격 게이트(100%)를 계속 "
                       "스치듯 탈락 — 마지막 세그먼트는 제약을 100%로 조여 동결 전선에서 통과자를 "
                       "찾는 중. 최종 후보(p22a 또는 동결 통과자)와 P19의 선택은 사용자 몫 — 전 데이터 "
                       "그래프 79종(g22_p22a_all_results)이 판단 자료다. 벽을 근본적으로 옮기는 건 "
                       "여전히 벤치 실측(질량 2종은 수학적으로 데이터 불가 판정).")),
        loc("후보 fourbar_p22a_candidate.json (judge p22, REPRODUCED) · 탐색 p22_nsga.py + ckpt · "
            "게이트 p22_gate_check.json · 그래프 Desktop/jump_opt/g22_p22a_all_results/ (79 png+gif)"),
    ])
    ok = N.verify_images(p)
    print("P22-5", p, "images:", ok)


if __name__ == "__main__":
    main()
