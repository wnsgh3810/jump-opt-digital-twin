# MARATHON_SEA — 디지털 트윈 대수술: 숨은 스프링(hip 직렬탄성) 도입 + 다과제 일반화

시작: 2026-07-28 · 커미션(사용자): "모델 크게 고쳐도 되고 시간·토큰 무제한. 수직점프만이 아니라
max payload sit2stand, horizontal jump까지 이 모델로 최적화할 것 — 여러 방면으로 digital twin이
잘되어야 함. 영상도 활용. Mode A든 CL이든 발전시켜라."

## 배경 (exp5 마라톤의 발견 — 이 마라톤의 출발점)

exp5(26.07.27, hip 7게인 스윕) + 영상 검증 + Mode A 검증 + 교차-세션(7일치)으로 **삼중 수렴 확정**:

> **hip 액추에이터와 사지 사이에 직렬 탄성(비틀림 스프링) k_s ≈ 160±20 Nm/rad가 실재한다.**
> ① α 램프 적합: α=k_s/(kp+k_s), k_s=150 (배포 CL 관점, 7게인 1-파라미터)
> ② Mode A e1~â1 회귀: k̂ 풀드 166, r +0.87~0.92 (플랜트 관점) — 보정 후 hip 4~5°→1~1.6°
> ③ FK 가짜슬립(3mm/Nm) + 영상 반증(진짜 슬립 ≤14mm) (기하 관점)
> 교차-세션: 배포 5일(07-22~27) 전부 재현 (16/19 r>0.7, k̂ 중앙 ~170). 저게인 k̂ 93~122 =
> 저부하 연화(비선형) 힌트. fit 세션(0424/0602)은 보정층이 흡수/과잉보정(현상 vs 물리 판별 완료).

파생 확정: 트윈 접촉 물리 무죄 (트윈 발중심 −13.8mm = 실물 영상 10~17mm 일치, 슬립 DOF 불필요).

## REJECTED 재심 사유 (철칙: 재시도 전 명기)

- **#28 링키지 탄성** — 재심 조건 "벤치에서 실측 강성 확보 시" **충족** (in-situ 3중 측정 k_s≈170).
- **#10 기어박스 SEA·#21 벨트 SEA** — 기각 근거가 "fit 세션 채점"이었는데, 이번에 fit 세션에선
  보정층이 스프링을 흡수해 신호가 안 보임을 입증 (0424 r 음수 = 과잉보정). 기각의 증거 기반 무효화.
  또한 당시 구현(로터 DOF+텐던, 동역학 내부)과 달리 이번 1차 구현은 **측정·제어 인터페이스면**
  (인코더=모터측)이라 실패 양식이 다름.
- **#11 hip 스프링(병렬)** — 별개 물리 (병렬 스프링=정적 토크 발생, 직렬탄성=정적 토크 0). 재심 불요.

## 지표 (Phase 1에서 수식 확정 예정 — 선고정 후 불변)

- **불가침 게이트 G**: 기존 bench p19 judge (FIT 36.1 / HO CL 33.1, tol ±0.5%) + held-out 0324 악화 금지.
- **M1 (배포 Mode A)**: 5일 19 trial의 Mode A hip RMSE 평균 [현행 ~6.2° 추정, 바닥 ~1.3°].
- **M2 (배포 CL 무α)**: exp5 7게인 라벨 게인 그대로(α 테이블 없이) CL 예측 — q RMSE + F_τ 예측오차 + |Δh|.
- **M3 (다과제)**: 26.06.04 페이로드 s2s Mode A 지표 (기존 정의 재사용).

## HYPOTHESES

| # | 가설 | 시험 | 판정 | 근거 수치 |
|---|---|---|---|---|
| H1 | 골든 재현 (p24a) | 정본 TW.golden() | **PASS** | ahat/CL미러 비트동일, 0602 재생 1.285 (기준 1.29±0.15). 단 bench.py eval 경로는 P24 env 플래그 미설정으로 FIT 59% 오표시 — bench 어댑터 수정 필요 (별도 태스크) |
| H2 | 스프링 형태: 선형 vs 저부하연화 vs 백래시+선형 | 풀드 e1-τ1 곡선 모델선택 | 대기 | |
| H2b | 경쟁가설 전수판별: 탄성(τ1)/지연(dq1)/스케일·중력(q1)/마찰(sgn·τ)/무릎·베이스(τ2) | 16 trial 판별회귀 | **탄성 승** | τ1 부호 16/16 일관, c1=0.339°/Nm→k11≈169. τ2 교차항 부호 비일관(+9/−7)=보정층 잔재 추정. 마찰=공선 허상(계수 부호 불안정), 스케일·중력=토크복귀 시 오차복귀로 반증, 지연=열세 |
| H3 | s2s 준정적(0604 페이로드)에서 같은 k_s | 지속토크 구간 변형 회귀 | | |
| H4 | 영상 키프레임 링크각 vs 인코더 직접 대조 | 최대토크 프레임 각도 측정 | **점프영상 판정불가→0604로 피벗** | 24fps 한계: 이륙 직전 40°/프레임이라 앵커 ±0.5프레임=±7° — 중반 f125 측정 −2.8°(밴드 −5.0~+1.4)는 예측 스케일과 무모순이나 비결정적. ★0604 페이로드 s2s(지속토크·정지)가 타이밍 무관 심판 — H3와 통합 |
| H5 | 측정면 스프링 도입으로 M1 붕괴 + G 불변 | 구현 A + bench | | |
| H6 | CL 무α: α 테이블 제거해도 exp5 재현 (α 창발) | 구현 A CL 롤아웃 | **v2 PASS ★α 창발 입증** | v1(대수적)=CL 공진 FAIL → v2(모터상태 SEA-lite: θ_m 컨트롤러측 적분, DOF 無, 인코더=θ_m, τ_link=k_s(θ_m−q)+b_s(dθ_m−dq)) — ★exp5 7게인 α=(1,1,1,1)·k_s=169로 현행 OLD α 완파: q1 12.30→7.00°(−43%), q2 12.98→9.5~9.8°(−26%), τ2 5.66→3.6~4.2(−30%), τ1 동급, 발산 0, b_s(0.5~3)·J_m(0.01~0.03) 둔감. α 테이블=스프링의 창발임이 CL에서 입증. sea_twin2.py/_h6v2.json. 잔여: 수렴 골든은 온건 극한으로 재검(1e5+0.005는 수치 한계) |
| H7 | 보정층 다이어트: 지지법칙 일부 → 스프링 대체로 fit 세션도 유지 | 공동 재적합 | | ★이것이 #10 기각의 재심판 — 여기서 못 이기면 과거 기각이 옳았던 것 |
| H8 | 관측면 배치(인코더식 교정)가 과거 SEA 기각(동역학 배치)을 해소 | 구현 A vs 과거 방식 비교 | | goal19 'series 기여 0'은 동역학 배치+관측식 미교정의 결과라는 화해 논리 검증 |
| H9 | 베이스/캐리지 피치 없음 (변형은 관절 내부) | 영상 몸통 피치 측정 (역대 미실험) | **PASS (베이스 무죄)** | 250 게인 캐리지 브래킷 2점 추적(매칭오차 0~176) — 몸통 라인 각도 변화 **0.00°** 전 스탠스. 변형은 사슬 내부 확정 |

## 로그

- 07-28: 마라톤 개시. Phase 0: REJECTED 재심 사유 명기, 정본 골든 PASS(0602 1.285), bench eval 플래그 결함 발견(별도 수정).
- 07-28: 영상 인벤토리 69개(전 세션, 0604 s2s 8개 포함). 오차 해부 16 trial: 탄성 승, 교차항=보정층 잔재 추정.
- 07-28: 과거 기록 채굴 서브에이전트 가동. 노션 허브(3abab81d-2550-81d4-848d-e9862bcd18de) + 자식 ①배경 ②기준점 ③오차해부.
- 07-28: 채굴 완료 — ★정면 모순 3건 발견·화해 논리 명기 (goal19 'series 기여 0'=동역학 배치+관측식 미교정 vs 이번=관측면 교정).
  설계 재료: p24a hip 보정층 = 유지법칙 λ(τ̂,v) + 무릎부하 커플링 b1=−0.26 (다이어트 1순위, 오차해부 교차항과 직결), K1_HIP≈0.
  P18b 무릎=병렬 스프링 0.404 (knee r 약함과 정합, hip=직렬/knee=병렬 비대칭). 베이스 피치 직접 실험 역대 없음 → H9 신설.
  백래시=동역학 넣으면 발산 이력 → 관측면 데드존으로만. OLD α=rollout 휴리스틱 기원 확인.
- 07-28: H9 PASS (몸통 피치 0.00°, 베이스 무죄 — 역대 첫 직접 실험). H4 점프영상=24fps 타이밍 한계로 비결정 (정직 기록)
  → 다음 심판 = 0604 페이로드 s2s 준정적 (지속토크+정지=타이밍 무관, H3+H4 통합). video_deflection.py/_video_deflection.json.
- 07-28: 0604 준정적 카탈로그 (s2s_static.py/_s2s_static.json, raw_unwrap 장기록 50~70s): 페이로드 6단, 정적 40+구간.
  ★신규 축 발견 — 0kg 무토크에서도 FK 발끝 136mm 자세의존 표류 = **각도 오프셋/기하 캘리브 잔차 실재** (탄성 아님).
  2단 식별 구조 확정: 무토크 정적→오프셋 식별, 하중 정적 잔여→탄성. 트라이얼 내 즉석 k̂≈126 후보 (기립 τ1 −3.94↔−2.40, 영상 확인 대기).
  cvt 트라이얼 포함(hip 유효), knee는 크랭크 변환 필요. 영상 정적 측정 서브에이전트 가동 중.
- 07-28: Mode A 클립 자체감사 — 기본 R19.CLIP=35.5 확인 (측정 raw 최대 37.2의 순간 5%만 접촉) → k̂ 추정 무결.
- 07-28: H6 v1 FAIL (정직 기록) — sea_twin.py 대수적 구현은 CL 공진. 미러골든 1.4e-08 (수치 동일, 비트 아님 — ks=1e12 잔차).
  v2 설계 = 모터상태 SEA-lite (위 H6 칸). OLD α 대조군 절대치도 나쁨(q1 12°, |Δh| 0.195 — h 정의 불일치 포함, 재정의 필요).
- 07-28: ★H6 v2 PASS — SEA2가 OLD α 완파 (q1 −43%·q2 −26%·τ2 −30%, 7게인 안정, 파라미터 둔감). α 창발 입증.
  다음(Phase 3): knee SEA 변형(650) 시험, 보정층(무릎커플링 b1) 다이어트 + 공동 재적합, 게이트(0602·0429·held-out) 전수, 수렴 골든 온건판.
